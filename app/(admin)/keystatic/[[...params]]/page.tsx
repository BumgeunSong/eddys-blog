// The catch-all page renders nothing itself — the Keystatic app is mounted by
// the sibling layout.tsx, which handles all /keystatic/* routes client-side.
export default function Page() {
  return null
}
